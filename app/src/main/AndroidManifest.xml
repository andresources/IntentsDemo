<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <queries>
        <package android:name="com.broadcastRevDemo" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntentsDemo"
        tools:targetApi="31" >
        <activity
            android:name=".ex3launchmodes.E_Activity"
            android:launchMode="singleTop"
            android:exported="false" />
        <activity
            android:name=".ex3launchmodes.D_Activity"
            android:exported="false" />
        <activity
            android:name=".ex3launchmodes.C_Activity"
            android:exported="false" />
        <activity
            android:name=".ex3launchmodes.B_Activity"
            android:exported="false"
            android:launchMode="singleTask" />
        <!--
<activity
            android:name=".ex3launchmodes.A_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        -->
        <activity
            android:name=".ex3launchmodes.A_Activity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>